{
  "name": "nomeo-theme",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nomeo — travel eSIM Shopify theme. Built on Dawn, restyled with Tailwind v4 + Vite.",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "dev": "shopify theme dev",
    "check": "shopify theme check",
    "clean": "rm -rf assets layout/theme.liquid layout/password.liquid sections snippets templates config locales"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "fast-glob": "^3.3.2",
    "tailwindcss": "^4.1.0",
    "vite": "^6.3.0"
  }
}
